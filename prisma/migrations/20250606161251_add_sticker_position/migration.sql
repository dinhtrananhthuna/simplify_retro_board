BEGIN TRY

BEGIN TRAN;

-- AlterTable
ALTER TABLE [dbo].[Sticker] ADD [position] INT NOT NULL CONSTRAINT [Sticker_position_df] DEFAULT 0;

COMMIT TRAN;

END TRY
BEGIN CATCH

IF @@TRANCOUNT > 0
BEGIN
    ROLLBACK TRAN;
END;
THROW

END CATCH
